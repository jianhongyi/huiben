<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport"
        content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0" />
    <title>悠游分级阅读App下载</title>
    <link rel="stylesheet" href="static/css/reset.css" />
    <script>
        (function (doc, win) {
            var docEl = doc.documentElement;
            var resizeEvt = 'orientationchange' in window ? 'orientationchange' : 'resize';
            var baseSize = 100;
            var recalc = function () {
                var width = docEl.clientWidth;
                docEl.dataset.width = width;
                docEl.dataset.percent = baseSize * (width / 750);
                docEl.style.fontSize = baseSize * (width / 750) + 'px';
            };
            recalc();
            if (!doc.addEventListener) return;
            win.addEventListener(resizeEvt, recalc, false);
        })(document, window);
    </script>
    <style>
        body,
        html {
            height: 100%;
        }

        body {
            background-color: -webkit-gradient(linear, left top, right top, from(#1c89ff), to(#007aff));
            background-color: linear-gradient(90deg, #1c89ff, #007aff);
            background-image: url(static/img/download/bg.png);
            background-size: cover;
            background-repeat: no-repeat
        }

        .container {
            padding-top: 2.5rem;
            text-align: center;
            color: #ffffff;
        }

        .logo {
            display: block;
            width: 1.92rem;
            height: 1.92rem;
            border-radius: 0.3rem;
            margin: auto;
            margin-bottom: 0.5rem;
        }

        .title {
            font-size: 0.6rem;
            margin-bottom: 0.3rem;
        }

        .subtitle {
            position: relative;
            font-size: 0.3rem;
        }

        .subtitle * {
            display: inline-block;
            vertical-align: middle;
        }

        .subtitle span {
            width: 1rem;
            height: 0.02rem;
        }

        .subtitle p {
            margin: 0 0.2rem;
        }

        .subtitle .left {
            background: -webkit-gradient(linear, right top, left top, from(rgba(255, 255, 255, 1)), to(rgba(255, 255, 255, 0)));
            background: -o-linear-gradient(right, rgba(255, 255, 255, 1) 0%, rgba(255, 255, 255, 0) 100%);
            background: linear-gradient(270deg, rgba(255, 255, 255, 1) 0%, rgba(255, 255, 255, 0) 100%);
        }

        .subtitle .right {
            background: -webkit-gradient(linear, left top, right top, from(rgba(255, 255, 255, 1)), to(rgba(255, 255, 255, 0)));
            background: -o-linear-gradient(left, rgba(255, 255, 255, 1) 0%, rgba(255, 255, 255, 0) 100%);
            background: linear-gradient(90deg, rgba(255, 255, 255, 1) 0%, rgba(255, 255, 255, 0) 100%);
        }

        #button {
            position: fixed;
            left: 0;
            right: 0;
            bottom: 0.8rem;
            width: 6.8rem;
            height: 1rem;
            line-height: 1rem;
            color: #3a7ff6;
            background-color: #ffffff;
            text-align: center;
            font-size: 0.4rem;
            margin: auto;
            border-radius: 0.5rem;
        }
    </style>
</head>

<body>
    <div class="container">
        <img class="logo" src="static/img/download/logo.png" alt="logo" />
        <p class="title">悠游分级阅读</p>
        <div class="subtitle">
            <span class="left"></span>
            <p>专注提升少儿英语阅读能力</p>
            <span class="right"></span>
        </div>
    </div>
    <div id="button">
        点击下载
    </div>

    <script>
        document.getElementById('button').addEventListener('click', function () {
            location.href = "https://apps.apple.com/cn/app/id1490218827";
        });
    </script>
</body>

</html>